#include "main.h"

void sumf(num, num2)
{
	return (num + num2);
}
void difff(num, num2)
{
	return (num - num2);
}
void mulf(num, num2)
{
	return (num * num2);
}
void dividef(num, num2)
{
	return (num / num2);
}
